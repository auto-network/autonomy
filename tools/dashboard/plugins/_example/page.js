// Example plugin — Alpine.data factory referenced by frontend.alpine_root.
window.examplePage = function () {
  return {
    init() {
      // Plugin substrate sample — no behavior beyond rendering placeholder.
    },
  };
};
