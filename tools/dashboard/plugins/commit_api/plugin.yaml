id: commit-api
api_version: 1
org: autonomy
default_enabled: false

paths:
  - /commit

assets:
  template: page.html
  script: page.js

nav:
  label: Commit API

frontend:
  alpine_root: commitApiPage

entrypoints:
  api: tools.dashboard.plugins.commit_api.entrypoints.api:routes

