[Service]
# Installed only by the TURN activation after the secret exists. The Registry
# discovers this systemd credential through CREDENTIALS_DIRECTORY; without the
# drop-in it starts normally and fails only TURN issuance, never link service.
LoadCredential=turn-rest-secrets:/etc/autonomy-coturn/turn-rest-secrets
