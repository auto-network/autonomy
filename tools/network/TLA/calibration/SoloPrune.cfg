\* Prune without every active peer's acknowledgement: a concurrently
\* enrolled machine with nothing consumed loses the replay it was owed.
SPECIFICATION Spec
CONSTANTS
    Peers = {p1, p2}
    Enrollee = q
    MaxRef = 3
    PruneByTimestamp = FALSE
    RequireFullRosterAck = FALSE
    AckResetOnInstall = TRUE
    GapRescue = FALSE
INVARIANTS
    DeltaAlwaysServable
