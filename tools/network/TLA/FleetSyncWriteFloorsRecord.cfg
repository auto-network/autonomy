\* The record as written (bead auto-mmwgu, Propagation). Expected: the
\* constitution's principle 1 (CursorHoldsData) is violated.
SPECIFICATION Spec
CONSTANTS
    Machines = {a, b, c}
    MaxRows = 1
    MaxClock = 3
INVARIANTS
    CursorHoldsData
PROPERTIES
    Converges
