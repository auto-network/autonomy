\* Shipped semantics: ref-order acks, full-roster floor, install reset,
\* continuity checkpoint rescue. Safety and liveness must both hold.
SPECIFICATION Spec
CONSTANTS
    Peers = {p1, p2}
    Enrollee = q
    MaxRef = 3
    PruneByTimestamp = FALSE
    RequireFullRosterAck = TRUE
    AckResetOnInstall = TRUE
    GapRescue = TRUE
INVARIANTS
    AckSoundness
    Recoverable
PROPERTIES
    EventuallyRetired
