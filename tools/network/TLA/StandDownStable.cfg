\* Candidate: an evicted connector recognizes 4409 and stands down. This
\* must converge and recover across one relay restart, while the split
\* register/evict safety properties remain true.
SPECIFICATION FairSpec
CONSTANTS
    ORGS = {"org1"}
    CONNECTORS = {"old", "new"}
    ConnectorOrg <- ScenConnectorOrg
    Version <- ScenVersion
    MinBackoff = 1
    MaxRetryBackoff = 2
    HardBackoff = 3
    Jitter = 1
    MaxDelay = 4
    EvictionPolicy = "standDown"
    RestartBudget = 1
INVARIANT TypeOK
INVARIANT HolderHasOpenSocket
INVARIANT NoUncausedVacuum
INVARIANT NoTerminalVacuum
PROPERTY EventuallyStable
PROPERTY EventuallyAvailable
