\* The correction at a wider bound: two rows per writer, two seals.
SPECIFICATION Spec
CONSTANTS
    Machines = {a, b, c}
    Writers = {a}
    MaxRows = 2
    MaxClock = 4
    Corrected = TRUE
INVARIANTS
    CursorHoldsData
PROPERTIES
    Converges
