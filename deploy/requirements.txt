# Runtime dependencies of the dashboard/graph home base (deploy/Dockerfile).
#
# Direct deps pinned to the versions proven in the dashboard agent image
# (agents/Dockerfile.dashboard) and the primary host, 2026-07-17. Update
# deliberately, in lockstep with those environments. Transitive deps are
# left to pip so security patches flow; the direct pins keep behavior
# reproducible where it matters.
#
# Installed from PyPI — anonymous, no account or token required
# (sovereign-distribution stance, see DEPLOY.md).

starlette==1.3.1
fastapi==0.139.2
uvicorn==0.44.0
# uvicorn --reload's file watcher. Without it uvicorn falls back to StatReload
# and prints "--reload-include and --reload-exclude have no effect unless
# watchfiles is installed" at every boot, and the entrypoint's --reload-dir/
# --reload-exclude scoping is silently ignored.
watchfiles==0.24.0
jinja2==3.1.6
httpx==0.28.1
pymysql==1.1.2
sse-starlette==3.3.4
inotify_simple==2.0.1
websockets==16.1
pyyaml==6.0.3
pydantic==2.13.4
cryptography==49.0.0
python-gnupg==0.5.6
# WebAuthn passkey ceremonies (tools/dashboard/identity_routes.py) —
# py_webauthn; imported lazily, endpoints 500 without it.
webauthn==3.0.0
# Imported directly by the WebAuthn contract tests as well as transitively by
# webauthn. Pin it explicitly so test collection never depends on resolver luck.
cbor2==6.1.3
# Standards-based RFC 8030/8291/8292 Web Push publication. The temporary
# operator-only iOS proof uses this instead of implementing content encryption
# and VAPID signing in Dashboard code. MPL-2.0, web-push-libs/pywebpush.
pywebpush==2.4.0
# RaptorQ (RFC 6330) codec for swarmkit fountain transfer — PyO3
# bindings of the Apache-2.0 cberner/raptorq crate. Attribution:
# "raptorq — Copyright Christopher Berner — Apache-2.0,
# github.com/cberner/raptorq" (no upstream NOTICE file).
raptorq==2.0.0

# ── WebRTC (aiortc) — ACTIVE, user-side installs only ────────────────────
# Transitive set for the frozen WebRTC stack selection
# (tools/network/ICE_STACK_SELECTION.md, auto-zg3ue). Resolved against the
# pins above on 2026-08-13; cryptography stays at 49.0.0. Dashboard suite
# verified in this exact environment: no failure attributable to aiortc/av.
#
# LICENSE DISPOSITION (operator, 2026-08-13): av's PyPI wheel bundles an
# FFmpeg that links GPL x264/x265 while self-labeling LGPL (brief:
# graph://42e64126-23d). Installing it user-side from PyPI is fine — we
# distribute nothing. STANDING RULE: av never ships inside an artifact we
# publish (prebuilt images exclude it or install it first-boot on the
# user's side). Revisit only if that rule ever has to break.
aiortc==1.15.0
aioice==0.10.2
av==17.1.0
pylibsrtp==1.0.0
pyee==13.0.1
ifaddr==0.2.0
dnspython==2.8.0
google-crc32c==1.8.0
pyOpenSSL==26.4.0
