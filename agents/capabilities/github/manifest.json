{
    "name": "autonomy/github",
    "version": 1,
    "implements": [
        {"contract": "source_control", "version": 1}
    ],
    "delivery_mode": "image_baked",
    "package_root": "agents/capabilities/github",
    "skill_path": "agents/capabilities/github/SKILL.md",
    "primer_path": "agents/capabilities/github/primer.md",
    "probe": {
        "kind": "command",
        "entrypoint": "gh auth status"
    },
    "required_env": ["GH_TOKEN"],
    "notes": "Implementation lives in agents/capabilities/github/service.py. Operations exposed under source_control@1: source_control.review.read, source_control.review.refresh, source_control.gates.watch_set. Per the v1 capability model (graph://86e04207-a25), review and merge-gate concerns nest under source_control rather than appearing as separate top-level contracts. Probe runs `gh auth status` inside the live workspace container."
}
