# The url is upstream's ROLLING latest-release archive; the sha256 pins one
# exact build. When upstream releases, the hash stops matching and installs
# fail closed — that is pin expiry, not compromise. Refresh deliberately:
# verify the new build, update sha256 + version together (see SKILL.md).
url=https://johnvansickle.com/ffmpeg/releases/ffmpeg-release-amd64-static.tar.xz
sha256=abda8d77ce8309141f83ab8edf0596834087c52467f6badf376a6a2a4c87cf67
version=7.0.2
